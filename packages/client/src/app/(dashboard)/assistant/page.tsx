import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AssistantPage() {
  return (
    <PlaceholderPage
      title="AI Assistant"
      description="Global AI Assistant is coming soon. Use the AI Assistant tab inside each audit for now."
    />
  );
}
